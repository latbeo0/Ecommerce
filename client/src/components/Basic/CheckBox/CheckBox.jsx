import React from 'react';
import { Container, Label, Input } from './CheckBoxStyled';

const CheckBox = () => {
    return (
        <Container>
            <Input />
            <Label>Label</Label>
        </Container>
    );
};

export default CheckBox;
